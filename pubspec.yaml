name: my_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.18.0
  fl_chart: ^0.63.0  # ←ここに移動

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 画像などのアセットを追加したいときは下を使う（今は不要ならコメントのままでOK）
  # assets:
  #   - images/

  # フォントを追加する場合の例（今は不要ならコメントのままでOK）
  # fonts:
  #   - family: ExampleFont
  #     fonts:
  #       - asset: fonts/ExampleFont-Regular.ttf
